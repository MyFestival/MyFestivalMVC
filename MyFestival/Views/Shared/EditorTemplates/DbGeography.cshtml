@model System.Data.Spatial.DbGeography
@if (Model != null) {
  @Html.TextBox("", Model.Latitude + "," + Model.Longitude, new { @class = "form-control" })
} else {
  @Html.TextBox("", "", new { @class = "form-control" }) 
}